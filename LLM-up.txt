You are a professional HTML poster designer. Optimize the generation logic of the given draft HTML poster into a polished final version. Follow the steps below strictly and output a complete, self-contained, fully English HTML file that can be run directly in a browser.

### 1. Global Visual Enhancement
- Maintain color harmony throughout.
- Container styles: `padding: 24px; border-radius: 8px;`.
- Slightly adjust font sizes for author and affiliation information to keep the layout clean.

### 2. Fix Grid Layout
- Ensure `grid-template-areas` is exactly:
  ```
  "motivation overview key_idea"
  "core core core"
  "contributions highlights project"
  ```
- **Do not** use additional grid areas such as `main_method` or `method_overview`; all content must be placed into the six areas above.
- If there are duplicate `core` areas, merge them into one, and rename it to "Core Results".
- Within the `core` area, use a three-column layout (`.core-grid`):
  - Left column: Result Figure 1
  - Middle column: Performance table (with data and dataset names; do not fabricate data if not provided)
  - Right column: Result Figure 2

### 3. Content Redistribution (Precise for Each Block)
- **Motivation**:
  - First paragraph: Point out the limitations of existing methods.
  - Second paragraph: Present the core advantages of this work.
  - Add a `formula-box` displaying a key formula.
  - Add a `callout` to highlight a key insight.
  - **Remove** any benchmark list content.
- **Method Overview**:
  - Briefly outline the method.
  - List two bullet points.
  - Insert (Network Architecture Overview) as a standalone image.
  - Below the image, add a bordered formula block showing the most important formula of the method.
- **Key Idea**:
  - Focus on the most critical aspect and explain how it is used.
  - Three bullet points.
  - Insert an illustration (Detailed Structure Diagram).
- **Core Results** (formerly the `core` area):
  - Change title to "Core Results".
  - Top line: a concise statement highlighting key performance metrics and advantages.
  - Three columns (`.core-grid`):
    - Left: (Visual Results) Result Figure 1
    - Middle: Table with three rows of data (include dataset names)
    - Right: (Visual Comparison and Ablation) Result Figure 2
- **Contributions**:
  - Four bullet points.
- **Highlights**:
  - Four short bullet points.
  - Add four `badge-pill` labels (you may include symbols like 🌐 ⚡ 🏆 🔄 🔥 🚩, etc.).
- **Project**:
  - QR placeholder (`[QR]`).
  - Code & Project tags, with a GitHub link shown below (if available in the paper; otherwise, state "code will be released").

### 4. Images and File References
- Use the following image files (assume they all exist):
  - `Result Figure 1` → placed in the left column of Core Results
  - `Result Figure 2` → placed in the right column of Core Results
  - `Model Framework (Network Architecture Overview)` → placed in Method Overview
  - `Detailed Structure Diagram` → placed in Key Idea
- **Remove** any other image references (e.g., extra result figures or content diagrams).

### 5. Miscellaneous
- Keep MathJax configuration (`inlineMath: [['$', '$']]`).
- Ensure all `.section-block` elements use consistent border-radius and border styles.
- Keep responsive design unchanged (`@media (max-width: 1100px)`).
- Highlight key terms appropriately.
- Overall content must be concise, and avoid duplication of content or images.
